{
  "name": "codex-spark-grid",
  "version": "1.0.0",
  "description": "Unified orchestration for Codex Spark Grid",
  "main": "server/server.js",
  "scripts": {
    "install-client": "cd client && npm install",
    "install-server": "cd server && npm install",
    "build-client": "cd client && npm run build",
    "render-build": "npm run install-server && npm run install-client && npm run build-client",
    "start": "cd server && node server.js"
  },
  "author": "",
  "license": "ISC"
}
